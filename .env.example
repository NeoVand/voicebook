# Dev-only provider keys. Copied into the running dev server so the premium
# paths are testable without typing secrets into the UI; production builds
# compile these out entirely (see src/lib/state/providers.svelte.ts).
VITE_DEV_ANTHROPIC_API_KEY=
VITE_DEV_OPENAI_API_KEY=
VITE_DEV_GEMINI_API_KEY=
VITE_DEV_ELEVENLABS_API_KEY=
